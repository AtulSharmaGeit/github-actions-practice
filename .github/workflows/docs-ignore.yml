name: Docs Ignore

on:
  push:
    branches:
      - main
      - release/*
    paths-ignore:
      - '*.md'
      - 'docs/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Triggered by code changes, not docs"'
